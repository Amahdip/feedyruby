{{- if and .Values.envoy.enabled .Values.envoy.feedyruby.ingress.enabled }}
{{- $explicitHost := "" -}}
{{- if .Values.envoy.feedyruby.ingress.host -}}
{{- $explicitHost = tpl .Values.envoy.feedyruby.ingress.host $ | trim -}}
{{- end -}}
{{- if and .Values.ingress.enabled .Values.ingress.hosts (gt (len .Values.ingress.hosts) 0) (not $explicitHost) -}}
{{- fail "envoy.feedyruby.ingress.host must be set explicitly when ingress.enabled and envoy.feedyruby.ingress.enabled are both true" -}}
{{- end -}}
{{- $host := include "feedyruby.envoy.ingressHost" . -}}
{{- $proxyServiceName := include "feedyruby.envoy.proxyServiceName" . -}}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-envoy-api" (include "feedyruby.name" .) }}
  namespace: {{ include "feedyruby.namespace" . }}
  labels:
    {{- include "feedyruby.labels" . | nindent 4 }}
  {{- if .Values.envoy.feedyruby.ingress.annotations }}
  annotations:
    {{- include "feedyruby.tplvalues.render" (dict "value" .Values.envoy.feedyruby.ingress.annotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.envoy.feedyruby.ingress.ingressClassName }}
  ingressClassName: {{ .Values.envoy.feedyruby.ingress.ingressClassName }}
  {{- end }}
  rules:
    - host: {{ $host }}
      http:
        paths:
          {{- if .Values.envoy.feedyruby.routes.authCallback }}
          - path: /api/auth/callback
            pathType: Prefix
            backend:
              service:
                name: {{ $proxyServiceName }}
                port:
                  number: 80
          {{- end }}
          {{- if .Values.envoy.feedyruby.routes.v1Client }}
          - path: /api/v1/client
            pathType: Prefix
            backend:
              service:
                name: {{ $proxyServiceName }}
                port:
                  number: 80
          {{- end }}
          {{- if .Values.envoy.feedyruby.routes.v2Client }}
          - path: /api/v2/client
            pathType: Prefix
            backend:
              service:
                name: {{ $proxyServiceName }}
                port:
                  number: 80
          {{- end }}
          {{- if .Values.envoy.feedyruby.routes.v1Management }}
          - path: /api/v1/management
            pathType: Prefix
            backend:
              service:
                name: {{ $proxyServiceName }}
                port:
                  number: 80
          {{- end }}
          {{- if .Values.envoy.feedyruby.routes.v1Webhooks }}
          - path: /api/v1/webhooks
            pathType: Prefix
            backend:
              service:
                name: {{ $proxyServiceName }}
                port:
                  number: 80
          {{- end }}
          {{- if .Values.envoy.feedyruby.routes.feedbackRecords }}
          - path: /api/v3/feedbackRecords
            pathType: Prefix
            backend:
              service:
                name: {{ $proxyServiceName }}
                port:
                  number: 80
          - path: /v1/feedback-records
            pathType: Prefix
            backend:
              service:
                name: {{ $proxyServiceName }}
                port:
                  number: 80
          {{- end }}
          {{- if .Values.envoy.feedyruby.routes.storage }}
          - path: /storage
            pathType: Prefix
            backend:
              service:
                name: {{ $proxyServiceName }}
                port:
                  number: 80
          {{- end }}
{{- end }}
